import { HttpsRequestInterceptor } from './https-request-interceptor';

describe('HttpsRequestInterceptor', () => {
  it('should create an instance', () => {
    expect(new HttpsRequestInterceptor()).toBeTruthy();
  });
});
